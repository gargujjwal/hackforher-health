export interface SupportGroup {
  name: string;
  description: string;
  website: string;
  frequency: string;
  format: string;
  registration: string;
}

export interface Benefit {
  icon: JSX.Element;
  title: string;
  content: string;
}
